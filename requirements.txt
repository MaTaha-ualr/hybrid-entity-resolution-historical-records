# Python dependencies for the hybrid ER pipeline (notebook + hm_taha_modules).
# Install: pip install -r requirements.txt
# Use Python 3.10+ recommended.

openai>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
sentence-transformers>=3.0.0
tqdm>=4.66.0
aiohttp>=3.9.0
jupyter>=1.0.0
